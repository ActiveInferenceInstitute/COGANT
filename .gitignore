# Coverage artifacts — all transient, regenerate via pytest
.coverage
.coverage.*
coverage.xml
htmlcov/
coverage.json

# pytest / hypothesis
.pytest_cache/
.hypothesis/

# Output artifacts
output/

# OS / editor cruft
.DS_Store
**/.DS_Store

# Cursor / IDE state (shell hook artifacts, project metadata)
.cursor/

# Accidental literal-tilde directory created by misquoted shell expansions
/~/
