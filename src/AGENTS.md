# AGENTS.md — COGANT template shell source

This `src/` directory exists for the parent `docxology/template` active-project contract.
The real COGANT implementation remains in [`../cogant/py/cogant/`](../cogant/py/cogant/).

Use [`cogant_template_contract.py`](cogant_template_contract.py) only for lightweight
template discovery and promotion checks. Do not add COGANT runtime logic here.
