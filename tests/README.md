# Template Shell Tests

This directory lets `docxology/template` discover COGANT as a structurally valid
active project after promotion. It does not replace the real package tests under
`../cogant/tests/`.

Run:

```bash
uv run pytest
```
