# AGENTS.md — COGANT template shell tests

These tests cover only the parent-template compatibility shell in [`../src/`](../src/).
The full package test suite lives in [`../cogant/tests/`](../cogant/tests/) and must
still be run from the inner package root with `uv run pytest tests/ -q`.
