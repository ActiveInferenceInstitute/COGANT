# Web Output Directory

This directory contains the HTML exports generated from the `manuscript/` Markdown sources for online or browser-based publication.

## Export Details

During pipeline execution, the source files are translated into corresponding `.html` payloads, including combined `index.html` indexes.
