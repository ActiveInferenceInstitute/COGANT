# Slides Output Directory

This directory contains the Beamer PDF slides generated from individual manuscript sections.

## Rendering Pipeline

Each section in `manuscript/` (e.g., `00_abstract.md`) emits a parallel `_slides.pdf` inside this directory during the build lifecycle, targeting a presentation format.
