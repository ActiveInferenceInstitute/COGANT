# Workspace Packages

Importable packages for the multi-package workspace fixture. `app_pkg` depends
on `core_pkg` so COGANT can observe cross-package imports, calls, and boundary
roles without relying on an external repository.
