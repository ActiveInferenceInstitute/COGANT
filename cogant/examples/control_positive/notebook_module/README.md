# Notebook Module Fixture

Plain Python module shaped like a notebook export: cell markers, exploratory
functions, and a final report object. It avoids `.ipynb` parsing in fast tests
while preserving notebook-converted structure.
