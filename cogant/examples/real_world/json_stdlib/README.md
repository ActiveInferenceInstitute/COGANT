# json_stdlib

Python stdlib `json` usage sample for static analysis exercises.

- `encoder.py`, `decoder.py`, `scanner.py` — JSON encode/decode paths
- Verifies COGANT handles standard-library patterns without false positives
- See [../AGENTS.md](../AGENTS.md) for evaluation context
