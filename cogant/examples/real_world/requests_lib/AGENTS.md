# Agents — examples/real_world/requests_lib

`requests` HTTP client sample for real-world ingest testing.

- `sessions.py`, `adapters.py`, `auth.py`, `models.py`, `utils.py` — requests internals subset
- Tests how COGANT handles network-layer code without Active Inference false positives
- Parent coordination: [../AGENTS.md](../AGENTS.md)
