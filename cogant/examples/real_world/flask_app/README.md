# flask_app

Minimal Flask web application used as a real-world ingest fixture.

- `app.py` — Flask factory; `models.py`, `services.py`, `utils.py` — domain layers
- `config.py` — environment-based configuration
- See [../AGENTS.md](../AGENTS.md) for evaluation context
