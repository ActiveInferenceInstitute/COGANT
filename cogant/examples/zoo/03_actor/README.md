# 03 Actor

A single action type with an `act()` method that mutates system state. This
exercises the ActionRule via the "set"/"update" keyword patterns and WRITES
edges. COGANT should produce at least one ACTION mapping.
