# 10 Constraint

Validation and constraint-checking functions that verify system invariants.
Exercises the PreferenceRule via "validate"/"check"/"assert_" patterns and
class-level "Checker"/"Validator" naming. COGANT should produce CONSTRAINT
mappings.
