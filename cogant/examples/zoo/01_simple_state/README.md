# 01 Simple State

A single hidden state factor with an `update_state` method. This is the
simplest possible Active Inference pattern: one belief vector that gets
updated over time. COGANT should map the class to HIDDEN_STATE and the
update method to ACTION.
