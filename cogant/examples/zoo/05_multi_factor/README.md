# 05 Multi-Factor

Two independent hidden state factors (temperature and pressure) tracked in a
single agent. Exercises HiddenStateRule twice, once per factor. COGANT should
produce two distinct HIDDEN_STATE mappings from the two `state`-like attributes.
