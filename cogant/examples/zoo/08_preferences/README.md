# 08 Preferences

An explicit preference/utility function that evaluates how desirable a given
state is. Exercises the PreferenceRule via "validate"/"check" keywords and
the ConstraintRule pattern. COGANT should map these to CONSTRAINT mappings.
