# 07 Event Driven

An event bus pattern with handler registration and dispatch. Exercises the
EventBusRule and PolicyRule (via "handler"/"dispatch" keywords). COGANT should
detect OBSERVATION (event subscription), ACTION (handler execution), and
POLICY (dispatcher routing).
