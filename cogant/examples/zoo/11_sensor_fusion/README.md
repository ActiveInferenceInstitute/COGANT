# 11 Sensor Fusion

Two observation modalities (visual and tactile) merged into a unified belief
update. Exercises ObservationRule twice (one per sensor) and ActionRule for
the fusion update step. COGANT should detect multiple OBSERVATION mappings
and at least one ACTION mapping.
