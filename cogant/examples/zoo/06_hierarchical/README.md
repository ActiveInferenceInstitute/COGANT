# 06 Hierarchical

Parent/child state hierarchy where a high-level planner state governs a
low-level executor state. Exercises HiddenStateRule at two levels of a
class hierarchy. COGANT should detect HIDDEN_STATE in both parent and child,
plus ACTION mappings for the update methods.
