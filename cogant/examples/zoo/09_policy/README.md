# 09 Policy

A policy module with `select_action` and `select_policy` functions. Exercises
the PolicyRule via the "dispatch"/"handle" keywords and function-level pattern
matching. COGANT should produce POLICY and ACTION mappings.
