# Cargo Configuration

Cargo configuration for the optional Rust acceleration workspace. The settings
here apply when commands run under [`../`](../), including `cargo check`,
`cargo test`, `cargo clippy`, and `maturin develop` for `cogant-ffi`.

Do not use this folder for crate code; crate implementations live in sibling
`cogant-*` directories.
