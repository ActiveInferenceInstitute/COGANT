# Package GitHub Metadata

Package-local GitHub metadata for standalone COGANT checkouts. The staging-root
[`../../.github/`](../../.github/) directory owns the active template workflows;
this folder exists so the inner package can also carry package-specific GitHub
configuration when promoted or split out.

## Contents

- [`workflows/`](workflows/) - package-root workflow definitions.
