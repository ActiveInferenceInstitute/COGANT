# Package Workflows

Workflow definitions intended for a standalone COGANT package checkout. The
current file, [`docs.yml`](docs.yml), builds MkDocs from the package root.

For the staging tree, the active workflow inventory is documented in
[`../../../.github/workflows/README.md`](../../../.github/workflows/README.md).
