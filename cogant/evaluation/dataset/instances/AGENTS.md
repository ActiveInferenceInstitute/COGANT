# Agents — evaluation/dataset/instances

## Purpose

Per-repository or per-run dataset instance files (JSONL shards, fixtures) consumed by `generate_dataset.py` / tests. Keep filenames stable where tests depend on them.

## Coordination

Dataset card and overview: [`../README.md`](../README.md), [`../DATASET_CARD.md`](../DATASET_CARD.md).
