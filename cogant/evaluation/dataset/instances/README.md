# instances

Per-repository dataset instance files consumed by the evaluation pipeline.

- `calculator.json`, `event_pipeline.json`, `flask_app.json`, `flask_mini.json` — Python examples
- `json_stdlib.json`, `requests_lib.json` — real-world repo instances
- Keep filenames stable where tests depend on them
- Parent: [../README.md](../README.md)
