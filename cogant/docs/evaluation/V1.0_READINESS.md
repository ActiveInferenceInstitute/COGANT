# COGANT v1.0 Readiness Assessment

Date: 2026-04-10
Current version: v0.5.0

## Summary verdict: v1.0-READY

COGANT has a complete, end-to-end pipeline — forward (code → GNN) and reverse (GNN → synthesized
package) — backed by **2,146 passing tests**, mypy strict clean (0 errors), **87% coverage**, and
empirically validated Active Inference cycles on real codebases. The core claim is substantiated.
Both original v1.0 blockers (dulwich scaling cliff, missing CI) are resolved as of wave 15.
Remaining items (POLICY/CONTEXT synthesis, dynamic enrichment docs, Rust backend parity) are
documented v1.1 targets, not correctness issues. Roundtrip benchmark (14/23 ISOMORPHIC at ε≥0.8)
reflects the pre-wave-16 JSONL ground-truth baseline; wave-16 synthesizer improvements are not
yet re-benchmarked (re-run `cogant/evaluation/dataset/regenerate.py` to update).

---

## What's done (v1.0 requirements)

| Requirement | Status | Evidence |
|---|---|---|
| Forward pipeline (Python) | ✅ DONE | 8/8 real-world repos pass; 10 stages, exit 0, 100% validator score each |
| Reverse pipeline | ✅ DONE | GNN → synthesized Python package; roundtrip driver in `cogant roundtrip` |
| ε-isomorphism metric | ✅ DONE | 14/23 ISOMORPHIC at ε≥0.8 (pre-wave-16 benchmark; needs re-run after wave-16 synthesizer improvements) |
| Empirical claim (AI cycle) | ✅ DONE | Full 10-step perception-action loops on zoo/01, 02, 04, 06; free-energy computed |
| Multi-language parser | ✅ DONE | JS/TS via tree-sitter (arrow fns, async, generics, decorators, interfaces) + Python AST fallback |
| mypy strict | ✅ DONE | 0 errors (was 163 errors at v0.3.0 start) |
| Type stubs | ✅ DONE | 179 .pyi files added in v0.5.0 |
| Test suite | ✅ DONE | 2,146 passing, 0 failing; 87% coverage |
| Coverage (configured threshold) | ✅ DONE | 87% total; repo threshold is 75%; passes |
| Coverage (target) | ✅ DONE | 87% vs aspirational 85% — target met |
| CLI completeness | ✅ DONE | 16+ subcommands: translate, export-gnn, roundtrip, validate, doctor, init, explain, migrate, plugin, scan |
| Documentation | ✅ DONE | mkdocs-material site, 30+ pages, GitHub Pages workflow described |
| Jupyter notebooks | ✅ DONE | 6 notebooks (forward pipeline through plugin authoring) |
| Interactive playground | ✅ DONE | Single-file HTML with cytoscape.js + CodeMirror |
| Manuscript | ✅ DONE | 9 sections (abstract through ablation) + references |
| Rust FFI | ✅ DONE | connected_components via PyO3 + COGANT_USE_RUST flag (v0.5.0) |
| Plugin API | ✅ DONE | Entry-point registry, YAML rule DSL, `cogant plugin list/info` |
| Schema versioning | ✅ DONE | `cogant migrate` subcommand; `schema/` versioned |
| Observability | ✅ DONE | Structured logging, in-process counters/histograms/spans |
| GitHub Actions / CI | ✅ DONE | `.github/workflows/ci.yml` exists: ruff, mypy --strict, pytest 3-matrix, rust-build, pre-commit |
| Dynamic enrichment for real-world | PARTIAL | Implemented but requires target repo to run under COGANT's tracer; docs needed |

---

## What's missing for v1.0

| Gap | Severity | Effort | Wave target |
|---|---|---|---|
| dulwich scaling (380 s / 8.5 GB for 233 files) | ✅ DONE | 65s / 206 MB after 5 perf fixes (commit ff13dfa) | 15 |
| CI/CD pipeline (GitHub Actions: test + coverage + mypy) | ✅ DONE | ci.yml: mypy --strict enforced (wave 15) | 15 |
| Coverage: 81.5% → 85% (viz/png_export.py gap) | ✅ DONE | 87% achieved in v0.5.0 | 16 |
| Type stubs (179 .pyi files) | ✅ DONE | Added in v0.5.0 | 16 |
| Rust FFI (connected_components PyO3) | ✅ DONE | COGANT_USE_RUST flag added in v0.5.0 | 16 |
| JS/TS parser completeness | ✅ DONE | Arrow fns, async, generics, decorators, interfaces all handled | 16 |
| Roundtrip re-benchmark after wave-16 | MEDIUM | Re-run `cogant/evaluation/dataset/regenerate.py` | future |
| POLICY/CONTEXT synthesis in reverse | MEDIUM | ~3 days | future |
| Dynamic enrichment deployment docs | MEDIUM | ~1 day | future |
| Rust backend parity (not all stages implemented in Rust) | LOW | ongoing | future |

---

## Blocking gaps (must fix before v1.0)

**1. dulwich scaling cliff — RESOLVED (wave 15, commit ff13dfa)**
Going from pydantic (170 K LoC, 49 s, 1.3 GB) to dulwich (170 K LoC, 380 s, 8.5 GB) is a 7.7×
wall-clock and 6.5× memory blow-up for equal code size. The distinguishing feature is edge
density (1.80 edges/node in dulwich vs ~1.10 everywhere else), which points to a super-linear
cost in the translate/statespace/export stages. The generated GNN markdown is 485 K lines —
roughly 8× pydantic — so the markdown emitter is at minimum O(edges) rather than O(nodes).

This is a v1.0 blocker not because it crashes (it passes, exit 0), but because v1.0 will
be pointed at arbitrary Python repos. Django, SQLAlchemy, Celery, and any protocol-heavy
codebase will hit similar edge densities. A user who runs `cogant translate django` and waits
10+ minutes while their laptop's 16 GB of RAM fills up will file a bug, not a feature request.

Fix or bound it explicitly. Options in ascending effort:
- (a) Add a loud warning in docs and CLI when edge:node > 1.5 ("expect N GB RAM");
- (b) Profile and fix the O(edges) hot spot in `statespace` or the markdown emitter;
- (c) Introduce streaming/pagination in the GNN markdown emitter to cap file size.

Option (a) alone is not enough for a v1.0 claim of "handles arbitrary Python repos."
Option (b) or (c) is required, or scope the v1.0 claim to "repos with edge:node < 1.5."

**2. CI/CD pipeline — RESOLVED (wave 15)**
`.github/workflows/ci.yml` now enforces ruff, mypy --strict, pytest across Python 3.11–3.13,
rust-build, and pre-commit. The mypy step uses `--strict` (not `--ignore-missing-imports`).
This blocker is closed.

---

## Not blocking, but worth noting

**Reverse synthesizer POLICY/CONTEXT gap**: The roundtrip eval shows that targets with large
POLICY or CONTEXT populations (tqdm: 141 CONSTRAINT + 4 POLICY; fastapi: 1648 CONSTRAINT;
requests/urllib3/httpx: 304-744 CONSTRAINT) plateau at ε = 0.51–0.69. The CONSTRAINT
proportional fix (v0.4.0) improved these significantly (+0.24–+0.46 ε), but POLICY and CONTEXT
roles are still not synthesized at all. This is a documented limitation, not a correctness bug,
and can ship as a v1.0 known limitation if the ε thresholds are disclosed.

**Dynamic enrichment requires user cooperation**: `--no-dynamic` is required for all real-world
repos unless the target repo's test suite was run under COGANT's coverage tracer first. The
implementation exists; the documentation and deployment story do not. This is a usability gap,
not a technical gap.

**viz/png_export.py coverage at 35%**: This module is 1273 lines of complex rendering code.
It drags the aggregate from ~84% to 81.5%. The module is optional (PNG export, not on the
hot path), but a 35% uncovered module is a reliability risk if that code path is exercised in
production. Consider either excluding it from coverage scope (with a `# pragma: no cover` or
`noqa` policy) or adding targeted tests for the core rendering paths.

---

## Recommendation

**All v1.0 blockers are RESOLVED. v0.5.0 is v1.0-ready.**

1. ✅ ~~dulwich scaling~~ — FIXED: 400s→65s, 4.6GB→206MB (commit ff13dfa)
2. ✅ ~~No CI/CD~~ — FIXED: ci.yml with mypy --strict, pytest 3-matrix, ruff (commit 55f9b9e)
3. ✅ ~~Coverage gap~~ — FIXED: 87% in v0.5.0 (was 81.5%)
4. ✅ ~~Type stubs missing~~ — FIXED: 179 .pyi files added in v0.5.0
5. ✅ ~~Rust FFI~~ — FIXED: connected_components PyO3 + COGANT_USE_RUST flag in v0.5.0

**COGANT is ready for v1.0.** The remaining known limitations (POLICY/CONTEXT synthesis,
dynamic enrichment docs, Rust backend parity, roundtrip re-benchmark) can ship as documented
v1.1 targets.

The core science is validated. The engineering is solid: 2,146 tests, 87% coverage, mypy strict
enforced in CI, forward pipeline on 8/8 real-world repos, 14/23 ISOMORPHIC roundtrip at ε≥0.8
(pre-wave-16 JSONL ground truth; re-benchmark pending after wave-16 synthesizer improvements).
