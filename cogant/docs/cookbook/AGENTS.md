# AGENTS.md — Cookbook module

Hands-on recipes (CLI, CI, Docker, reverse synthesis, plugins). Each numbered `.md` file should stay self-contained; the [README.md](README.md) table is the canonical index.

## Maintenance

- Add new recipes as `NN_slug.md` and extend the README table.
- Link to CLI/API docs under `../cli/` and `../api/` instead of pasting long excerpts.
