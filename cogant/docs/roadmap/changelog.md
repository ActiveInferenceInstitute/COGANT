# Changelog

The canonical changelog is **[Changelog](../changelog.md)** (MkDocs) / `CHANGELOG.md` at the package repository root.

**Do not add release notes here.** Edit `CHANGELOG.md` at the package root, then refresh the published copy:

```bash
# from the package root (directory containing py/, docs/, CHANGELOG.md)
cp CHANGELOG.md docs/changelog.md
```

This page remains so compatibility links to `roadmap/changelog.md` still resolve.
