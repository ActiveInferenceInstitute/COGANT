# AGENTS.md — Guides module

Longer guided narratives beyond the cookbook.

- `first_project.md` — extended narrative complement to `../getting-started/` (full translate → roundtrip → interpret cycle on `examples/zoo/01_simple_state`). Named `first_project.md` rather than `getting_started.md` to avoid overlap with the `../getting-started/` section.
- Prefer linking to `../tutorials/` for walkthroughs and `../cookbook/` for short recipes
- Avoid duplicating tutorial steps here; cross-link instead
