# AGENTS.md — Concepts module

Conceptual primers (program graph, GNN, Markov blanket, round-trip, role assignment). Edit fragments in this directory; keep cross-links to `../architecture/` and `../theory/` README indexes.

## Maintenance

- Link to sibling modules via `../<module>/README.md` where a TOC exists.
- Do not duplicate full API reference; use `../api/README.md`.
