# JavaScript parser maintenance

Keep grammar loading lazy and keep import-time behavior safe on minimal
installs. Test JavaScript and JSX extensions, parser-unavailable behavior, and
source-span preservation whenever grammar or registry logic changes.
