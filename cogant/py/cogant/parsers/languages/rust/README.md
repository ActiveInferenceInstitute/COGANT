# Rust parser

`RustLanguageParser` is an experimental structural parser for Rust source.
Its output is useful for exploratory graph extraction but is not a claim of
complete Rust syntax coverage.

Expand it through fixture-backed increments and keep unsupported constructs
visible in parser capability and error reports.
