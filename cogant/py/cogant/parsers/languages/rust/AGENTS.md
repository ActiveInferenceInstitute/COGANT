# Rust parser maintenance

Treat this front end as experimental. Add a focused fixture before changing
symbol, import, method, async, macro, or test classification behavior, and
preserve explicit diagnostics for syntax the structural parser cannot model.
