# TypeScript parser

This package contains the TypeScript structural compatibility parser and the
optional tree-sitter implementation. The two paths must remain distinguishable
in capability metadata and exported evidence.

Use the registry rather than importing either implementation directly from a
caller. Add fixtures for `.ts` and `.tsx` syntax when behavior changes.
