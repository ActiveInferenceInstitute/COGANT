# Roundtrip Golden Fixtures

JSON expectations for selected forward-reverse-forward fixtures. Each file
captures role counts, state-space shape expectations, and the v0.6
role-preservation floor used by integration tests.

These are not generated blindly. Update them only after intentionally changing
roundtrip semantics and verifying the new invariant ledger.
